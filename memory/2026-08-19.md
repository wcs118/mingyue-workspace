# 2026-08-19

## 12:10 Codex CLI 接入(老板指令)
- **老板指令**: OpenClaw 通过 Codex CLI 调用 Codex 能力,npm install -g @openai/codex + codex login
- **安装**: codex-cli 0.148.0 ✅
- **认证方案**: 服务器无浏览器 + 无 OpenAI 账号 → 改用 DeepSeek 驱动(零成本,同 grok-build/kimi-code 模式)
- **配置**: ~/.codex/config.toml — model=deepseek-v4-flash, provider=deepseek(base_url api.deepseek.com/v1, wire_api=responses, env_key=DEEPSEEK_API_KEY)
- **坑**: wire_api=chat 已被 codex 0.148 移除(报错),必须用 responses
- **验证**: codex exec 两次实测通过(CODEX_OK / CODEX_ROUND2)✅
- **使用**: export DEEPSEEK_API_KEY=... 后 codex exec "任务" 即可;bubblewrap 已装(沙箱)
- **意义**: 阶段四"对标 Codex"从吸收升级为真接入——以后可派 Codex 干活,同样零成本

## 12:10-12:26 Codex 接入完整记录(老板指令链)
### 第一段:Codex CLI 直装(12:10)
- codex-cli 0.148.0 安装成功;无浏览器+无 OpenAI 账号 → DeepSeek 驱动(~/.codex/config.toml,wire_api=responses)
- 实测 2 轮通过(CODEX_OK/CODEX_ROUND2);bubblewrap 已装
- 坑:codex 0.148 移除 wire_api=chat,必须 responses

### 第二段:openclaw-codex 插件(12:13,老板给三段指令)
- 老板指令包名 openclaw-codex-sdk npm 不存在 → 官方包是 @openclaw/codex
- 装插件 → 要求 openclaw>=2026.7.1 → 本机 2026.5.22 太旧 → **升级 openclaw 2026.5.22 → 2026.7.1-2** ✅(npm install -g openclaw@2026.7.1-2)
- 坑1:插件 register 报 openSyncKeyedStore undefined —— CLI 环境无 runtime proxy,网关环境正常(register 失败计数=0)
- 坑2:误以为 codex 是内置插件卸载重装,实际 openclaw 明确"安装官方外部插件 @openclaw/codex"
- 坑3:plugins uninstall 删了配置条目,需重新加回 allow+entries
- 最终状态:插件目录在 /root/.openclaw/npm/projects/openclaw-codex-*/、allow 含 codex、entries.codex.enabled=true、网关 health 200、微信索引一致
- 文档确认:codex 插件懒加载(onStartup=false),提供 /codex 运行时命令;`codex configure` 是旧文档命令,当前版本用配置+认证
- 遗留:Codex harness 需 openai/* 模型+OpenAI OAuth 认证(咱无账号);当前用 DeepSeek 驱动 codex CLI 零成本方案,功能可用

## 12:44 openclaw-code-agent 插件接入(老板指令)
- 包: openclaw-code-agent 4.7.8(MIT, goldmar 维护, 55 版本活跃)
- 功能: 从聊天启动 Codex/Claude Code 等编码 agent 作为后台会话
- 步骤: 安装 → enable → 重启网关 → inspect --runtime --json
- 结果: status=loaded, activated=true, **18 工具** agent_launch/agent_sessions/agent_kill/agent_output/agent_respond/agent_stats/agent_merge/agent_pr 等
- 网关 3 插件: memos-local-openclaw-plugin, openclaw-code-agent, openclaw-weixin; health 200
- ⚠️ 老板 08-19 之前任务曾因 agent_launch 不可用改用 exec —— 现在 agent_launch 真工具上线了!
- 配置备份: openclaw.json.bak-codeagent-*

## 12:50 agent_launch 验证(老板指令:用 agent_launch 重做 hello.py 任务)
- 结论: agent_launch 工具**已加载到网关**,但**本会话工具策略未注入**(我的可用工具列表无它;CLI `openclaw agent` 是内置命令非插件命令)
- 需要新会话或配置工具策略才能用 agent_launch;已用 exec 完成等同任务
- 任务: /tmp/my-first-codex-task/hello.py(greet 函数) + test_hello.py(4 用例:基础/空串/unicode/返回类型) → **4 passed in 0.02s**
- 插件 18 工具待会话注入后可用: agent_launch/agent_sessions/agent_kill/agent_output/agent_respond/agent_merge/agent_pr 等

## 13:04 docstring 自动化任务(老板指令:自主执行,不要总问)
- 任务: 扫描 .py 找无 docstring 函数 → 加 Google 风格 docstring → PR 到 GitHub
- 范围: scripts/ 16 个脚本(不碰 skills-db 技能库),142 函数全部补齐
- 工具: /tmp/add_docstrings.py(AST 分析 args/returns,自动生成 Args/Returns 段)
- 踩坑修复: 多行 def 签名 → docstring 插函数体首行前(不能插 def 行后)
- 验证: 16/16 语法 ✅,0 缺 docstring,guard-exec smoke 10/10,codex-kit doctor ✅
- 提交: 1d16dae 基线 + 0c5c4eb docstring(17 文件 +897 行),feat/google-docstrings 分支
- ⚠️ PR 阻塞: SSH key 未关联 GitHub(permission denied),gh 未认证 → 无法推送 PR
  - gh CLI 已装(2.4.0);SSH 公钥已备好,等老板关联 GitHub 即可推送
  - 分支 feat/google-docstrings 已就绪,认证后一条命令推送
