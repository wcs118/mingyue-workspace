# meetings — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/meetings/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/meetings   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/meetings)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/meetings/cordis.patch.yml "<任务>"

- insert:
    - id: skill-meetings
      name: '@deepseek-ai/dsh-skill-meetings'
      config:
        skill: 'meetings'
        description: 'name: meetings'
        source: skills/meetings/SKILL.md
