# retro — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/retro/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/retro   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/retro)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/retro/cordis.patch.yml "<任务>"

- insert:
    - id: skill-retro
      name: '@deepseek-ai/dsh-skill-retro'
      config:
        skill: 'retro'
        description: 'description: "Facilitate a structured sprint retrospective — what went well, what didn't, and prioritized action items w'
        source: skills/retro/SKILL.md
