# sprint-plan — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/sprint-plan/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/sprint-plan   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/sprint-plan)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/sprint-plan/cordis.patch.yml "<任务>"

- insert:
    - id: skill-sprint-plan
      name: '@deepseek-ai/dsh-skill-sprint-plan'
      config:
        skill: 'sprint-plan'
        description: 'name: sprint-plan'
        source: skills/sprint-plan/SKILL.md
