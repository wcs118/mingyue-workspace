# launch-strategy — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/launch-strategy/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/launch-strategy   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/launch-strategy)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/launch-strategy/cordis.patch.yml "<任务>"

- insert:
    - id: skill-launch-strategy
      name: '@deepseek-ai/dsh-skill-launch-strategy'
      config:
        skill: 'launch-strategy'
        description: 'name: "launch-strategy"'
        source: skills/launch-strategy/SKILL.md
