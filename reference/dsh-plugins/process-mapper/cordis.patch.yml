# process-mapper — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/process-mapper/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/process-mapper   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/process-mapper)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/process-mapper/cordis.patch.yml "<任务>"

- insert:
    - id: skill-process-mapper
      name: '@deepseek-ai/dsh-skill-process-mapper'
      config:
        skill: 'process-mapper'
        description: 'name: process-mapper'
        source: skills/process-mapper/SKILL.md
