# writing-rules — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/writing-rules/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/writing-rules   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/writing-rules)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/writing-rules/cordis.patch.yml "<任务>"

- insert:
    - id: skill-writing-rules
      name: '@deepseek-ai/dsh-skill-writing-rules'
      config:
        skill: 'writing-rules'
        description: 'name: writing-hookify-rules'
        source: skills/writing-rules/SKILL.md
