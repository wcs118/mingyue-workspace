# sql-queries — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/sql-queries/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/sql-queries   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/sql-queries)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/sql-queries/cordis.patch.yml "<任务>"

- insert:
    - id: skill-sql-queries
      name: '@deepseek-ai/dsh-skill-sql-queries'
      config:
        skill: 'sql-queries'
        description: 'name: sql-queries'
        source: skills/sql-queries/SKILL.md
