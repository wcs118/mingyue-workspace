# fable-judge — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/fable-judge/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/fable-judge   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/fable-judge)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/fable-judge/cordis.patch.yml "<任务>"

- insert:
    - id: skill-fable-judge
      name: '@deepseek-ai/dsh-skill-fable-judge'
      config:
        skill: 'fable-judge'
        description: 'name: fable-judge'
        source: skills/fable-judge/SKILL.md
