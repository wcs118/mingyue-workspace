# cs-intent-classify — 明月技能 → dsh 插件(自动生成,2026-08-16)
# 转换自: skills/cs-intent-classify/SKILL.md
# 安装: cd /opt/dsh && pnpm add /root/.openclaw/workspace/reference/dsh-plugins/cs-intent-classify   (或 dsh plugin add /root/.openclaw/workspace/reference/dsh-plugins/cs-intent-classify)
# 加载: dsh --profile headless --patch /root/.openclaw/workspace/reference/dsh-plugins/cs-intent-classify/cordis.patch.yml "<任务>"

- insert:
    - id: skill-cs-intent-classify
      name: '@deepseek-ai/dsh-skill-cs-intent-classify'
      config:
        skill: 'cs-intent-classify'
        description: 'name: cs-intent-classify'
        source: skills/cs-intent-classify/SKILL.md
